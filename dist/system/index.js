'use strict';

System.register([], function (_export, _context) {
  "use strict";

  function configure(config) {
    config.globalResources(['./simple-tabs', './simple-tab.html']);
  }

  _export('configure', configure);

  return {
    setters: [],
    execute: function () {}
  };
});